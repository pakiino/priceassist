# Price Assist

## What this does
(TODO: 2-3 sentences — the resale pricing assistant idea)

## How it's organized
- main.py — entry point
- data/ — comps_dataset_template.csv, raw_sold_listings_all.csv
- tests/ — tests

## Dependencies
See requirements.txt. Install with:
    pip install -r requirements.txt

## How to run
(TODO: exact command to run the app)

## How to run tests
    python -m unittest discover -s tests -p "test_*.py" -v
